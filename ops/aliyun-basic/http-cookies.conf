# This deployment is explicitly HTTP/IP. Preserve HttpOnly and SameSite.
# Remove this mount when migrating the public entrypoint to HTTPS.
proxy_cookie_flags tap_token nosecure;
proxy_cookie_flags tap_refresh_token nosecure;
proxy_cookie_flags tap_session_present nosecure;
